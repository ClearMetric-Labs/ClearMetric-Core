id: lineage-demo
label: Lineage-demo downstream impact (lineage-demo project)
mode: project
project_dir: ../../../lineage-demo
outputs:
  - compile_format: json
    out: graph.json
  - compile_format: catalog
    out: catalog.json
impacts:
  - selection: orders_base.amount
    direction: downstream
    manifest_key: orders_base.amount_downstream
    out: impacts/orders_base.amount_downstream.json
defaults:
  impact_key: orders_base.amount_downstream
checks: checks.yaml
