Hand-traced from `sql/model.sql` with schema in `schema.json`.

SQL-folder input names the model dataset `sql.model` (path `sql/model.sql`).
Expected edge: `sql.model.customer_id` derives from `raw_customers.id`.

If this expectation changes, document the SQL re-trace here first.
